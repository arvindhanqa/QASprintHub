<UserControl x:Class="QASprintHub.Views.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <ScrollViewer VerticalScrollBarVisibility="Auto">
        <StackPanel Margin="20">
            <!-- Save Button -->
            <ui:Button Content="Save Settings" Command="{Binding SaveSettingsCommand}"
                      Appearance="Primary" Margin="0,0,0,20" HorizontalAlignment="Left" Padding="30,10"/>

            <!-- Sprint Defaults -->
            <GroupBox Header="SPRINT DEFAULTS" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="Sprint Duration:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBox Text="{Binding SprintDurationDays}" Width="50"/>
                        <TextBlock Text="working days" VerticalAlignment="Center" Margin="10,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- App Behavior -->
            <GroupBox Header="APP BEHAVIOR" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <CheckBox Content="Minimize to system tray on close" IsChecked="{Binding MinimizeToTray}" Margin="0,0,0,5"/>
                    <CheckBox Content="Start minimized to tray" IsChecked="{Binding StartMinimized}" Margin="0,0,0,5"/>
                    <CheckBox Content="Launch on Windows startup" IsChecked="{Binding LaunchOnStartup}"/>
                </StackPanel>
            </GroupBox>

            <!-- Notifications -->
            <GroupBox Header="NOTIFICATIONS" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <CheckBox Content="Show watcher notification on startup" IsChecked="{Binding ShowWatcherNotification}" Margin="0,0,0,5"/>
                    <CheckBox Content="Notify when sprint is ending (1 day before)" IsChecked="{Binding ShowSprintEndingNotification}" Margin="0,0,0,5"/>
                    <CheckBox Content="Notify on watcher swap" IsChecked="{Binding ShowSwapNotification}"/>
                </StackPanel>
            </GroupBox>

            <!-- Data -->
            <GroupBox Header="DATA" Margin="0,0,0,20">
                <StackPanel Margin="10">
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                        <TextBlock Text="Database Location:" VerticalAlignment="Center" Margin="0,0,10,0"/>
                        <TextBlock Text="{Binding DatabaseLocation}" VerticalAlignment="Center" Foreground="Gray"/>
                    </StackPanel>
                    <StackPanel Orientation="Horizontal">
                        <ui:Button Content="Backup Database" Command="{Binding BackupDatabaseCommand}" Margin="0,0,10,0"/>
                        <ui:Button Content="Restore Database" Command="{Binding RestoreDatabaseCommand}" Margin="0,0,10,0"/>
                        <ui:Button Content="Open Data Folder" Command="{Binding OpenDataFolderCommand}"/>
                    </StackPanel>
                </StackPanel>
            </GroupBox>

            <!-- About -->
            <GroupBox Header="ABOUT">
                <StackPanel Margin="10">
                    <TextBlock Text="QA Sprint Hub v1.0.0" FontWeight="Bold"/>
                    <TextBlock Text="Internal QA Tool for Sprint Management" Foreground="Gray" Margin="0,5,0,0"/>
                </StackPanel>
            </GroupBox>
        </StackPanel>
    </ScrollViewer>
</UserControl>
