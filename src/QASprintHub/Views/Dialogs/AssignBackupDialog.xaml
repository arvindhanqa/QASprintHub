<Window x:Class="QASprintHub.Views.Dialogs.AssignBackupDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Assign Backup Watcher"
        Height="400" Width="500"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowStyle="None"
        Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0" Text="Assign Backup Watcher" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>

        <!-- Select Backup -->
        <TextBlock Grid.Row="1" Text="Select Backup Watcher:" FontWeight="SemiBold" Margin="0,0,0,10"/>

        <ListBox Grid.Row="2" x:Name="MembersListBox" Margin="0,0,0,15">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}" Padding="5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <ui:Button Content="Cancel" Appearance="Secondary" Click="Cancel_Click" Margin="0,0,10,0" Padding="20,8"/>
            <ui:Button Content="Assign" Appearance="Primary" Click="OK_Click" Padding="20,8"/>
        </StackPanel>
    </Grid>
</Window>
