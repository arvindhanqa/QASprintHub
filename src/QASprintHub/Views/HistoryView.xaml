<UserControl x:Class="QASprintHub.Views.HistoryView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
             mc:Ignorable="d"
             d:DesignHeight="600" d:DesignWidth="800">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="Sprint History" FontSize="20" FontWeight="Bold" Margin="0,0,0,10"/>

        <DataGrid Grid.Row="1" ItemsSource="{Binding Sprints}"
                  AutoGenerateColumns="False"
                  CanUserAddRows="False">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Sprint" Binding="{Binding Sprint.DisplayName}" Width="*"/>
                <DataGridTextColumn Header="Watcher" Binding="{Binding Sprint.Watcher.Name}" Width="150"/>
                <DataGridTextColumn Header="PRs" Binding="{Binding TotalPRs}" Width="60"/>
                <DataGridTextColumn Header="Merged" Binding="{Binding MergedPRs}" Width="80"/>
                <DataGridTextColumn Header="Blocked" Binding="{Binding BlockedPRs}" Width="80"/>
                <DataGridTextColumn Header="Status" Binding="{Binding Sprint.Status}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <ui:Button Grid.Row="2" Content="Export to Excel" Command="{Binding ExportToExcelCommand}"
                   HorizontalAlignment="Left" Margin="0,10,0,0"/>
    </Grid>
</UserControl>
